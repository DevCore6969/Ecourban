package com.ecohub.app.ui.theme

import androidx.compose.ui.graphics.Color

// Vibrant primary colors (Greens)
val GreenVibrant = Color(0xFF00E676)  // Bright green
val GreenPrimary = Color(0xFF00C853)  // Standard green
val GreenDark = Color(0xFF009624)     // Deep green
val GreenLight = Color(0xFF5EFF9E)    // Light bright green

// Accent colors (Blues with increased saturation)
val BlueAccent = Color(0xFF2979FF)    // Bright blue
val BlueBrilliant = Color(0xFF00B0FF) // Electric blue
val BlueDeep = Color(0xFF0091EA)      // Deep saturated blue
val BlueLight = Color(0xFF80D8FF)     // Light bright blue

// Additional accent colors for features
val OrangeAccent = Color(0xFFFF9100)  // Bright orange
val PurpleAccent = Color(0xFFAA00FF)  // Bright purple  
val YellowAccent = Color(0xFFFFEA00)  // Bright yellow
val TealAccent = Color(0xFF1DE9B6)    // Bright teal

// Neutral colors
val DarkGray = Color(0xFF212121)
val Gray = Color(0xFF757575)
val LightGray = Color(0xFFEEEEEE)
val NearWhite = Color(0xFFFAFAFA)

// Surface colors
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF121212)

// Semantic colors (with increased vibrancy)
val SuccessGreen = Color(0xFF00E676)  // Bright success green
val ErrorRed = Color(0xFFFF1744)      // Bright error red
val WarningYellow = Color(0xFFFFEA00) // Bright warning yellow
val InfoBlue = Color(0xFF00B0FF)      // Bright info blue

// Gradient colors
val GradientStart = Color(0xFF00E676)
val GradientMid = Color(0xFF00B0FF)
val GradientEnd = Color(0xFFAA00FF) 